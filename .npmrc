# Handle optional dependencies properly
optional=true
# Ensure proper platform-specific dependencies
platform=linux
arch=x64
# Allow platform-specific dependencies to be installed
target_platform=linux
target_arch=x64
# Ignore platform-specific dependency warnings
ignore-scripts=false
# Allow optional dependencies to fail without breaking the build
optional-dependencies=true
